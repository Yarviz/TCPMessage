#include <iostream>
#include <string.h>

#include "server.h"

int main(int argc, const char **argv)
{
    //if (argc > 1 && strcmp(argv[1], "-server") == 0)
    std::cout << "Start Server!" << std::endl;
    Server server;

    return 0;
}
