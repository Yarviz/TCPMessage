#include <iostream>
#include <string.h>

#include "client.h"

int main(int argc, const char **argv)
{
    //if (argc > 1 && strcmp(argv[1], "-server") == 0)
    std::cout << "Start Client!" << std::endl;
    Client client;

    return 0;
}
